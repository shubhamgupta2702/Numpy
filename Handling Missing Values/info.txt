built in Numpy functions
1. np.isnan --> detect missing values
2. np.nan_to_num() --> replace the missing values to another numbers
3. np.isinf() --> use to detect infinite values

NaN -> not a number.